# Base de datos PostgreSQL (Neon, Supabase, etc.)
DATABASE_URL=postgresql://usuario:password@host:puerto/dbname?sslmode=require

# Clave secreta para firmar JWT (asegúrate de usar una fuerte en producción)
JWT_SECRET=una_clave_super_segura

# Entorno de ejecución
NODE_ENV=development
